<?php

namespace App\Enums;

/**
 * Note: respondent_status in the database is a free-text household role field
 * (e.g. "Ibu rumah tangga", "Kepala keluarga"), not an enumerable status.
 * This enum is left empty — remove or populate if fixed values are introduced.
 */
enum RespondentStatus: string
{
    //
}
